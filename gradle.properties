# Gradle Properties
org.gradle.jvmargs = -Xmx1G
org.gradle.parallel = true

# Mod Properties
version = 0.0.3
maven_group = io.github.laylameower
archives_base_name = laylafied

# Dependencies are managed at gradle/libs.versions.toml
